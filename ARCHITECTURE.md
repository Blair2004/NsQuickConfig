# Module Architecture & Flow Diagram

## 🏗️ Architecture Overview

```
┌─────────────────────────────────────────────────────────────┐
│                    NexoPOS Core System                       │
│                   (Laravel + Vue 3 + Vite)                   │
└────────────────────┬────────────────────────────────────────┘
                     │
                     │ Event: RenderFooterEvent
                     │ Route: ns.dashboard.home
                     ▼
┌─────────────────────────────────────────────────────────────┐
│              NsQuickConfig Module                            │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │          Backend (Laravel/PHP)                     │    │
│  │                                                     │    │
│  │  ┌──────────────────────────────────────┐         │    │
│  │  │   RenderFooterListener               │         │    │
│  │  │   • Listens for dashboard load       │         │    │
│  │  │   • Checks wizard completion         │         │    │
│  │  │   • Injects wizard view              │         │    │
│  │  └────────────┬─────────────────────────┘         │    │
│  │               │                                     │    │
│  │               ▼                                     │    │
│  │  ┌──────────────────────────────────────┐         │    │
│  │  │   QuickConfigController              │         │    │
│  │  │   • saveStep()                       │         │    │
│  │  │   • completeWizard()                 │         │    │
│  │  │   • saveStoreIdentity()              │         │    │
│  │  │   • fetchPrinters()                  │         │    │
│  │  │   • testPrint()                      │         │    │
│  │  │   • setupDefaultPrinter()            │         │    │
│  │  └────────────┬─────────────────────────┘         │    │
│  │               │                                     │    │
│  │               ▼                                     │    │
│  │  ┌──────────────────────────────────────┐         │    │
│  │  │   Options Service                    │         │    │
│  │  │   • Read/Write settings              │         │    │
│  │  │   • Store wizard state               │         │    │
│  │  └──────────────────────────────────────┘         │    │
│  └─────────────────────────────────────────────────────    │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │          Frontend (Vue 3 + TypeScript)             │    │
│  │                                                     │    │
│  │  ┌──────────────────────────────────────┐         │    │
│  │  │   WizardPopup.vue                    │         │    │
│  │  │   • Main wizard container            │         │    │
│  │  │   • Step navigation logic            │         │    │
│  │  │   • Progress tracking                │         │    │
│  │  └────────────┬─────────────────────────┘         │    │
│  │               │                                     │    │
│  │               ├─────────────────┐                  │    │
│  │               │                 │                  │    │
│  │  ┌────────────▼──────┐  ┌──────▼──────────┐      │    │
│  │  │ WelcomeStep.vue   │  │ StoreIdentity   │      │    │
│  │  │ • Welcome message │  │ Step.vue        │      │    │
│  │  │ • Overview        │  │ • Store config  │      │    │
│  │  │ • Skip option     │  │ • Currency      │      │    │
│  │  └───────────────────┘  └─────────────────┘      │    │
│  │               │                 │                  │    │
│  │  ┌────────────▼──────┐  ┌──────▼──────────┐      │    │
│  │  │ PrinterConfig     │  │ AppSuggestion   │      │    │
│  │  │ Step.vue          │  │ Step.vue        │      │    │
│  │  │ • Server setup    │  │ • Gastro card   │      │    │
│  │  │ • Printer list    │  │ • Authorizer    │      │    │
│  │  │ • Test print      │  │ • Finish setup  │      │    │
│  │  └───────────────────┘  └─────────────────┘      │    │
│  └─────────────────────────────────────────────────────    │
└─────────────────────────────────────────────────────────────┘
```

## 📊 Data Flow

```
┌───────────┐
│   User    │
│  Browser  │
└─────┬─────┘
      │
      │ 1. Navigate to /dashboard
      ▼
┌─────────────────┐
│  Laravel Route  │
│  ns.dashboard.  │
│      home       │
└────────┬────────┘
         │
         │ 2. Fires RenderFooterEvent
         ▼
┌────────────────────────┐
│ RenderFooterListener   │
│ • Check route name     │
│ • Query wizard status  │
└────────┬───────────────┘
         │
         │ 3. Wizard not complete?
         ▼
┌────────────────────────┐
│  Inject wizard.blade   │
│  • Load Vite assets    │
│  • Initialize Vue app  │
└────────┬───────────────┘
         │
         │ 4. Render Vue components
         ▼
┌────────────────────────┐
│   WizardPopup.vue      │
│   • Show as popup      │
│   • Display step 1     │
└────────┬───────────────┘
         │
         │ 5. User interaction
         ▼
┌────────────────────────┐
│   Step Components      │
│   • Capture input      │
│   • Validate data      │
└────────┬───────────────┘
         │
         │ 6. API request
         ▼
┌────────────────────────┐
│  nsHttpClient          │
│  POST /api/ns-quick-   │
│       config/*         │
└────────┬───────────────┘
         │
         │ 7. Process request
         ▼
┌────────────────────────┐
│ QuickConfigController  │
│ • Validate input       │
│ • Save to database     │
└────────┬───────────────┘
         │
         │ 8. Update options
         ▼
┌────────────────────────┐
│   Options Service      │
│   nexopos_options      │
│   • Store settings     │
└────────┬───────────────┘
         │
         │ 9. Return response
         ▼
┌────────────────────────┐
│   Vue Component        │
│   • Show success       │
│   • Next step / Close  │
└────────────────────────┘
```

## 🔄 Wizard Flow

```
┌─────────────────┐
│  Step 1:        │
│  Welcome        │
│  • Introduction │
│  • Overview     │
└────────┬────────┘
         │
         │ Click "Let's Get Started!"
         ▼
┌─────────────────┐
│  Step 2:        │
│  Store Identity │
│  • Store name   │
│  • Email        │
│  • Logo         │
│  • Currency     │
└────────┬────────┘
         │
         │ Click "Continue" → API: saveStoreIdentity
         ▼
┌─────────────────┐
│  Step 3:        │
│  Printer Setup  │
│  • Server addr  │
│  • Load printers│
│  • Test print   │
│  • Set default  │
└────────┬────────┘
         │
         │ Click "Set as Default & Continue" → API: setupDefaultPrinter
         │ OR Click "Skip printer setup for now"
         ▼
┌─────────────────┐
│  Step 4:        │
│  App Suggestions│
│  • Gastro       │
│  • Authorizer   │
│  • External lnks│
└────────┬────────┘
         │
         │ Click "Finish Setup" → API: completeWizard
         ▼
┌─────────────────┐
│  Wizard         │
│  Complete! ✅   │
│  Close popup    │
└─────────────────┘
```

## 🗄️ Database Schema

```
nexopos_options table:
┌──────────────────────────────────────┬──────────┬─────────────┐
│ key                                  │ value    │ Description │
├──────────────────────────────────────┼──────────┼─────────────┤
│ ns_quick_config_wizard_completed     │ boolean  │ Is done?    │
│ ns_quick_config_current_step         │ integer  │ Last step   │
│ ns_quick_config_completed_at         │ datetime │ When done   │
│ ns_store_name                        │ string   │ Store name  │
│ ns_store_email                       │ string   │ Store email │
│ ns_store_square_logo                 │ string   │ Logo URL    │
│ ns_currency_symbol                   │ string   │ $ € £ etc   │
│ ns_currency_position                 │ string   │ before/after│
│ ns_currency_prefered                 │ string   │ 'symbol'    │
│ ns_currency_precision                │ integer  │ 0-4         │
│ ns_pa_server_address                 │ string   │ Print URL   │
└──────────────────────────────────────┴──────────┴─────────────┘

nexopos_printers table: (if NsPrintAdapter is installed)
┌──────────────┬──────────┬─────────────────────┐
│ Column       │ Type     │ Description         │
├──────────────┼──────────┼─────────────────────┤
│ id           │ integer  │ Primary key         │
│ name         │ string   │ Display name        │
│ identifier   │ string   │ System name         │
│ interface    │ string   │ network/usb/etc     │
│ type         │ string   │ epson/star/etc      │
│ is_default   │ boolean  │ Default printer?    │
│ status       │ string   │ enabled/disabled    │
│ author       │ integer  │ User ID             │
└──────────────┴──────────┴─────────────────────┘
```

## 📡 API Endpoints Map

```
┌─────────────────────────────────────────────────────────────┐
│  Base: /api/ns-quick-config/                                │
│  Middleware: auth:sanctum                                   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  POST /save-step                                            │
│  ├─ Body: { step: number }                                 │
│  └─ Returns: { status, message }                           │
│                                                             │
│  POST /complete-wizard                                      │
│  ├─ Body: {}                                                │
│  └─ Returns: { status, message }                           │
│                                                             │
│  POST /save-store-identity                                  │
│  ├─ Body: {                                                 │
│  │    ns_store_name: string (required)                     │
│  │    ns_store_email: string                               │
│  │    ns_store_square_logo: string                         │
│  │    ns_currency_symbol: string (required)                │
│  │    ns_currency_position: string                         │
│  │    ns_currency_precision: integer (required)            │
│  │  }                                                       │
│  └─ Returns: { status, message }                           │
│                                                             │
│  POST /fetch-printers                                       │
│  ├─ Body: { server_address: string }                       │
│  └─ Returns: { status, data: [...printers] }               │
│                                                             │
│  POST /test-print                                           │
│  ├─ Body: {                                                 │
│  │    server_address: string,                              │
│  │    printer_name: string                                 │
│  │  }                                                       │
│  └─ Returns: { status, message }                           │
│                                                             │
│  POST /setup-default-printer                                │
│  ├─ Body: {                                                 │
│  │    server_address: string,                              │
│  │    printer: { name, displayName, ... }                  │
│  │  }                                                       │
│  └─ Returns: { status, message }                           │
└─────────────────────────────────────────────────────────────┘
```

## 🎨 Component Hierarchy

```
WizardPopup.vue (Root Component)
├── Header
│   ├── Title (dynamic based on step)
│   └── Close Button
│
├── Body (Step Container)
│   └── <component :is="currentStepComponent">
│       │
│       ├── WelcomeStep.vue
│       │   ├── Welcome Icon
│       │   ├── Welcome Message
│       │   ├── Feature List
│       │   └── Action Buttons
│       │       ├── Skip Setup
│       │       └── Let's Get Started
│       │
│       ├── StoreIdentityStep.vue
│       │   ├── Form Fields
│       │   │   ├── Store Name (text input)
│       │   │   ├── Store Email (email input)
│       │   │   ├── Store Logo (ns-field media)
│       │   │   ├── Currency Symbol (text input)
│       │   │   ├── Currency Position (select)
│       │   │   └── Decimal Precision (select)
│       │   └── Continue Button
│       │
│       ├── PrinterConfigStep.vue
│       │   ├── Server Address Input
│       │   ├── Load Printers Button
│       │   ├── Printer List (dynamic)
│       │   │   └── Printer Card (clickable)
│       │   ├── Action Buttons
│       │   │   ├── Test Print
│       │   │   └── Set as Default & Continue
│       │   └── Skip Link
│       │
│       └── AppSuggestionStep.vue
│           ├── App Cards Grid
│           │   ├── Gastro Card
│           │   │   ├── Icon
│           │   │   ├── Title
│           │   │   ├── Description
│           │   │   ├── Features List
│           │   │   └── Learn More Button
│           │   │
│           │   └── Authorizer Card
│           │       ├── Icon
│           │       ├── Title
│           │       ├── Description
│           │       ├── Features List
│           │       └── Download Button
│           │
│           └── Finish Setup Button
│
└── (No Footer - buttons in body)
```

## 🔐 Security & Validation

```
┌─────────────────────────────────────┐
│  Frontend Validation                │
├─────────────────────────────────────┤
│  • Required field checks            │
│  • Email format validation          │
│  • Numeric range validation         │
│  • Real-time error display          │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  API Middleware                     │
├─────────────────────────────────────┤
│  • auth:sanctum                     │
│  • Authenticated user required      │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  Backend Validation                 │
├─────────────────────────────────────┤
│  • Laravel validation rules         │
│  • Type checking                    │
│  • Range validation                 │
│  • Error message generation         │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  Database Persistence               │
├─────────────────────────────────────┤
│  • Options Service                  │
│  • Transaction safety               │
│  • Data sanitization                │
└─────────────────────────────────────┘
```

## 📦 Build Process

```
Source Files (TypeScript + Vue)
         │
         │ npm run dev / npm run build
         ▼
    Vite Bundler
         │
         ├─ TypeScript Compilation
         ├─ Vue SFC Compilation
         ├─ CSS Processing (Tailwind)
         ├─ Asset Optimization
         └─ Tree Shaking
         │
         ▼
  Public/build/
         │
         ├─ .vite/manifest.json
         └─ assets/main-[hash].js
         │
         ▼
  Blade Template (wizard.blade.php)
         │
         └─ @vite() directive loads assets
         │
         ▼
  Browser Renders Vue App
```

---

This architecture follows NexoPOS best practices and provides a scalable foundation for the Quick Store Configuration wizard.
